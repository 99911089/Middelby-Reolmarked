<Window x:Class="Reolmarked.View.TenantInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Indtast kundeoplysninger" Height="250" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Navn -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,5">
            <Label Content="Navn:" Width="100"/>
            <TextBox x:Name="NameTextBox" Width="250"/>
        </StackPanel>

        <!-- Email -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,5">
            <Label Content="Email:" Width="100"/>
            <TextBox x:Name="EmailTextBox" Width="250"/>
        </StackPanel>

        <!-- Telefon -->
        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0,5">
            <Label Content="Telefon:" Width="100"/>
            <TextBox x:Name="PhoneTextBox" Width="250"/>
        </StackPanel>

        <!-- Knapper -->
        <StackPanel Orientation="Horizontal" Grid.Row="3" Margin="0,10" HorizontalAlignment="Center">
            <Button Content="OK" Width="100" Margin="5" Click="Ok_Click"/>
            <Button Content="Annuller" Width="100" Margin="5" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
