<Window x:Class="Reolmarked.View.CustomerInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Kunde" Height="250" Width="400" WindowStartupLocation="CenterOwner">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Navn -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,5">
            <TextBlock Text="Navn:" Width="100" VerticalAlignment="Center"/>
            <TextBox x:Name="NameTextBox" Width="200"/>
        </StackPanel>

        <!-- Email -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,5">
            <TextBlock Text="Email:" Width="100" VerticalAlignment="Center"/>
            <TextBox x:Name="EmailTextBox" Width="200"/>
        </StackPanel>

        <!-- Telefon -->
        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0,5">
            <TextBlock Text="Telefon:" Width="100" VerticalAlignment="Center"/>
            <TextBox x:Name="PhoneTextBox" Width="200"/>
        </StackPanel>

        <!-- Knapper -->
        <StackPanel Orientation="Horizontal" Grid.Row="3" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="OK" Width="100" Margin="5" Click="Ok_Click"/>
            <Button Content="Annuller" Width="100" Margin="5" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
